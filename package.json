{
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vitepress-export-pdf": "^1.0.0"
  },
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "export-pdf": "press-export-pdf export .",
    "deploy-beta": "npm run docs:build; firebase use dev; firebase deploy --only hosting:beta",
    "deploy-prod": "npm run docs:build; firebase use prod; firebase deploy --only hosting:prod"
  },
  "dependencies": {
    "medium-zoom": "^1.1.0"
  }
}
